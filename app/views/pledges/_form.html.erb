<%= form_for :pledges do |f| %>
  <% if @pledge.errors.any? %>
    <div class="error_messages">
      <h2>Form is invalid</h2>
      <ul>
        <% for message in @pledge.errors.full_messages %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :Amount %><br/>
    <%= f.number_field :Amount_in_cents %>
  </div>
  
  <div class="actions">
    <%= f.submit :class => 'button [radius button]' %>
  </div>
<% end %>